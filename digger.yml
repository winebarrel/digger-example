projects:
  - name: tf
    dir: tf
    opentofu: true

# apply が成功したら PR を自動でマージする
auto_merge: true

# 同じプロジェクトを複数の PR から同時に触らせない。
# backendless では DynamoDB の DiggerDynamoDBLockTable に記録される。
pr_locks: true
